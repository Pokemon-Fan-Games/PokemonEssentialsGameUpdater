name: Create release

on:
  release:
    types:
      - created

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Zip Folder
      run: zip -r "PokeUpdater.zip" . -i "Plugins/*" "poke_updater/*" "config_files/pu_config" "config_files/pu_locales.json"

    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: "PokeUpdater.zip"
        generate_release_notes: true
        make_latest: true
        token: ${{ secrets.RELEASE_TOKEN_PLUGINS }}